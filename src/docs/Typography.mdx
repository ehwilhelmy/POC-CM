import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Foundations/Typography" />

# Typography

Two font families and a consistent type scale defined in `src/theme/tokens.css`.

## Font Families

<div style={{ display: 'flex', flexDirection: 'column', gap: 24 }}>
  <div>
    <div style={{ fontSize: 12, color: '#808080', marginBottom: 4 }}>--font-family-title</div>
    <div style={{ fontFamily: "'Jaini Purva', serif", fontSize: 32 }}>
      Jaini Purva — Display & Titles
    </div>
  </div>
  <div>
    <div style={{ fontSize: 12, color: '#808080', marginBottom: 4 }}>--font-family-body</div>
    <div style={{ fontFamily: "-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif", fontSize: 16 }}>
      System Sans-Serif — Body & UI Text
    </div>
  </div>
</div>

---

## Type Scale

<div style={{ display: 'flex', flexDirection: 'column', gap: 16 }}>
  {[
    { token: '--font-size-xxxl', size: '32px', label: 'XXXL' },
    { token: '--font-size-xxl', size: '24px', label: 'XXL' },
    { token: '--font-size-xl', size: '20px', label: 'XL' },
    { token: '--font-size-lg', size: '18px', label: 'LG' },
    { token: '--font-size-md', size: '16px', label: 'MD' },
    { token: '--font-size-sm', size: '14px', label: 'SM' },
    { token: '--font-size-xs', size: '12px', label: 'XS' },
  ].map(t => (
    <div key={t.token} style={{ display: 'flex', alignItems: 'baseline', gap: 16 }}>
      <div style={{ width: 48, fontSize: 11, color: '#808080', flexShrink: 0 }}>{t.label}</div>
      <div style={{ width: 48, fontSize: 11, color: '#a0a0a0', flexShrink: 0 }}>{t.size}</div>
      <div style={{ fontSize: t.size, fontFamily: "-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif", lineHeight: 1.3 }}>
        The quick brown fox jumps
      </div>
    </div>
  ))}
</div>

---

## Font Weights

<div style={{ display: 'flex', flexDirection: 'column', gap: 12 }}>
  {[
    { token: '--font-weight-regular', weight: 400, label: 'Regular' },
    { token: '--font-weight-medium', weight: 500, label: 'Medium' },
    { token: '--font-weight-semibold', weight: 600, label: 'Semibold' },
    { token: '--font-weight-bold', weight: 700, label: 'Bold' },
  ].map(w => (
    <div key={w.token} style={{ display: 'flex', alignItems: 'baseline', gap: 16 }}>
      <div style={{ width: 80, fontSize: 11, color: '#808080', flexShrink: 0 }}>{w.label}</div>
      <div style={{ width: 32, fontSize: 11, color: '#a0a0a0', flexShrink: 0 }}>{w.weight}</div>
      <div style={{ fontSize: 16, fontWeight: w.weight, fontFamily: "-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif" }}>
        The quick brown fox jumps over the lazy dog
      </div>
    </div>
  ))}
</div>

---

## Line Heights

| Token | Value | Use |
|-------|-------|-----|
| `--line-height-tight` | 1.25 | Headings, compact UI |
| `--line-height-normal` | 1.5 | Body text |
| `--line-height-relaxed` | 1.75 | Long-form reading |

### Usage

```css
.cm-heading {
  font-family: var(--font-family-body);
  font-size: var(--font-size-xxl);
  font-weight: var(--font-weight-bold);
  line-height: var(--line-height-tight);
}
```
